[package]
name = "qlink-game"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT OR Apache-2.0"

[lib]
name = "qlink_game"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "1.1"
