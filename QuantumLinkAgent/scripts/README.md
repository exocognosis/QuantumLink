# Scripts

This directory is reserved for QuantumLink Agent development scripts.

Expected future scripts:

- Generate typed schema bindings.
- Validate example policy files.
- Produce redacted fixture bundles.
- Run Agent-only unit checks.

Scripts should not mutate macOS, Windows, SteamOS, or `qlink-core` state unless explicitly designed as cross-silo integration tooling.
