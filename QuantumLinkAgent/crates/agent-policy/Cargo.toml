[package]
name = "qlink-agent-policy"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
qlink-agent-contracts = { path = "../agent-contracts" }
serde = { version = "1", features = ["derive"] }
