# UI Contracts

UI contracts define how platform clients present Agent recommendations.

Initial responsibilities:

- Render explanation cards.
- Render approval prompts.
- Render policy diffs.
- Render rollback guidance.
- Keep user identity, device identity, wallet identity, route policy, and mesh trust visibly separate.

Native macOS, Windows, and SteamOS UI implementation remains in the platform silos.
